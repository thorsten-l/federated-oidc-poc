
services:

  fop-org-b-kcdb:
    image: postgres:${POSTGRES_VERSION}
    restart: unless-stopped
    container_name: fop-org-b-kcdb
    environment:
      - POSTGRES_PASSWORD=keycloak
      - POSTGRES_USER=keycloak
      - POSTGRES_DB=keycloak
    volumes:
      - ./var/org-b/kcdb:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak"]
      interval: 5s
      timeout: 5s
      retries: 5

  fop-org-b-keycloak:
    image: quay.io/keycloak/keycloak:${KEYCLOAK_VERSION}
    restart: unless-stopped
    container_name: fop-org-b-keycloak
    command: >
      start 
      --debug 
      --log-level=INFO,org.apache.http:DEBUG,org.keycloak.services.managers:DEBUG,l9g:TRACE,org.hibernate:INFO,org.keycloak.transaction:INFO,org.keycloak.saml:TRACE,org.keycloak.protocol.oidc:TRACE
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin123
      - KC_DB=postgres
      - KC_DB_PASSWORD=keycloak
      - KC_DB_USERNAME=keycloak
      - KC_DB_URL_HOST=fop-org-b-kcdb
      - KC_DB_URL_PORT=5432
      - KC_DB_URL_DATABASE=keycloak
      - KC_HTTP_ENABLED=true
      - KC_HOSTNAME_STRICT=false
      - KC_PROXY_HEADERS=xforwarded
      - KC_HEALTH_ENABLED=true
      - JAVA_OPTS_APPEND=-Xmx4g -Xms1g
    volumes:
      - ./HealthCheck.java:/HealthCheck.java:ro
    depends_on:
      fop-org-b-kcdb:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'java /HealthCheck.java http://localhost:9000/health/ready']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  fop-org-b-nginx:
    image: nginx:1
    container_name: fop-org-b-nginx
    hostname: idp-org-b.dev.sonia.de
    restart: unless-stopped
    ports:
      - "30443:30443"
    volumes:
# certs
      - ./certs:/etc/kccerts:ro
# configuration
      - ./data/org-b/kcfront/etc/proxy.conf:/etc/nginx/proxy.conf:ro
      - ./data/org-b/kcfront/etc/ssl.conf:/etc/nginx/ssl.conf:ro
      - ./data/org-b/kcfront/etc/idp-org-b.dev.sonia.de.conf:/etc/nginx/conf.d/idp-org-b.dev.sonia.de.conf:ro
# various data
      - ./var/org-b/kcfront/cache:/share/container/nginx/cache
      - ./var/org-b/kcfront/log:/var/log/nginx
    environment:
      - TZ=Europe/Berlin
    healthcheck:
      test: curl -k --fail --head https://localhost:30443/ || exit 1
      interval: 10s
      timeout: 30s
      retries: 3
    depends_on:
      fop-org-b-keycloak:
        condition: service_healthy
    networks:
      default:
        aliases:
          - idp-org-b.dev.sonia.de

  fop-org-b-portal:
    image: ghcr.io/thorsten-l/l9g-oidc-portal:latest
    container_name: fop-org-b-portal
    hostname: portal-org-b.dev.sonia.de
    restart: unless-stopped
    volumes:
      - ./certs/dev.sonia.de/server.p12:/workspace/server.p12:ro
      - ./data/org-b/portal/config.yaml:/workspace/config.yaml:ro
      - ./data/org-b/portal/templates:/workspace/templates:ro
    environment:
      - TZ=Europe/Berlin
    ports:
      - "30444:30444"
    depends_on:
      fop-org-b-nginx:
        condition: service_healthy
    networks:
      default:
        aliases:
          - portal-org-b.dev.sonia.de

networks:
  default:
    name: fop
    external: true
